% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ngr_str_viewer_cog.R
\name{ngr_str_viewer_cog}
\alias{ngr_str_viewer_cog}
\title{Generate an HTML iframe for the COG Viewer}
\usage{
ngr_str_viewer_cog(
  url,
  v = 2L,
  viewer_base = "https://viewer.a11s.one/",
  width = "100\%",
  height = "600"
)
}
\arguments{
\item{url}{\link{character} A single string specifying the full URL of the COG file (e.g., an S3 HTTPS URL).}

\item{v}{\link{integer} Cache-busting version parameter appended to the viewer URL. Default is \code{2L}. Bump this in the
package when \code{viewer.html} has breaking changes (e.g., TiTiler path updates) to force browsers to fetch the
latest viewer for all reports on rebuild.}

\item{viewer_base}{\link{character} A single string specifying the base URL of the viewer. Default is
\code{"https://viewer.a11s.one/"}.}

\item{width}{\link{character} A single string specifying the iframe width. Default is \code{"100\%"}.}

\item{height}{\link{character} A single string specifying the iframe height in pixels. Default is \code{"600"}.}
}
\value{
\link{character} A single string containing the HTML \verb{<iframe>} tag. Pass to \code{\link[knitr:asis_output]{knitr::asis_output()}} for
rendering in report chunks.
}
\description{
Creates an HTML \verb{<iframe>} tag that embeds the COG viewer at \code{viewer.a11s.one} with a cache-busting version
parameter. Bump \code{v} when the viewer has breaking changes to invalidate stale browser caches across all reports.
}
\details{
The viewer URL is constructed as \verb{<viewer_base>?v=<v>&cog=<url>}. The \code{v} parameter is ignored by the viewer
JavaScript but changes the URL enough to bypass stale browser caches.
}
\examples{
ngr_str_viewer_cog("https://stac-dem-bc.s3.us-west-2.amazonaws.com/dem_pouce_coupe_2023.tif")

# In a report chunk:
# knitr::asis_output(ngr_str_viewer_cog("https://bucket.s3.amazonaws.com/ortho.tif"))

}
\seealso{
Other string: 
\code{\link{ngr_str_dir_from_path}()},
\code{\link{ngr_str_extract_between}()},
\code{\link{ngr_str_link_url}()}
}
\concept{string}
